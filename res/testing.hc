// This is a testing source class
// that includes some object that
// are loaded from 'test.hc'.

/* This is a basic class with a name
 * this class can be created with the
 * keyword new
 *
 * 'new Object()'
 */
class Object {
	int a = 10;
	
}

export void print(char* val) {
	char* buffer = &(0xb8000);
	
	
}